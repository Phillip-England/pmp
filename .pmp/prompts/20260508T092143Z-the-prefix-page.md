---
title: "the prefix page"
timestamp: 2026-05-08T09:21:43Z
---

# the prefix page

we should have a new page added to this application called the prefix page the prefix page has an input and it allows us to change a document that is a prefix markdown document it basically is a document that gets prefixed on every single compilation here's the use case let's imagine that you want something to be appended to every single compilation output at the very beginning well that is what the prefix Pages for you can go to this page and you can basically set up what you would like to be prefixed to every prompt and you can save it there it is just a simple page with an input that displays the current prefix and a text area and allows the user to change it
