---
title: "Indicate the marked prompt in the web UI"
timestamp: 2026-05-08T08:24:27Z
---

# Indicate the marked prompt in the web UI

The web UI and interface should visually indicate which prompt is the marked prompt on all pages any page where the marked prompt is visually visible. It should be indicated that it is the currently marked prompt maybe by put making it a different color or something like that, but it should be visualeasy to tell which prompt was the last prompted. The project was compiled on because that is basically what the marked prompt is for.
