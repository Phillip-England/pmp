---
title: "make the web view the primary way of accepting user input"
timestamp: 2026-05-08T09:12:37Z
---

# make the web view the primary way of accepting user input

right now we have this whole system where we are connected to VI or our default text editor but since we have implemented this nice web view that allows us to input prompts into the system we know longer need the ability to write prompts using VI or our default text editor we don't need that sort of thing anymore so now whenever a user runs PMP just automatically open up the web UI to the page where we can enter in a new prompt for the system we no longer need that to actually open up a text editor also there should be no reason to have to initialize a new project if a user runs PMP just go ahead and initialize the project for them in the current directory there's no need for them to manually need to do that so let's go ahead and get all of that done please and thank you
