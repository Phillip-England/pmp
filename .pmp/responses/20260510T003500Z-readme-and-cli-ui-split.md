---
title: "README and CLI/UI split documented"
timestamp: 2026-05-10T00:35:00Z
---

# README and CLI/UI split documented

Added a new root README that matches the current codebase instead of the older spec text. It now explains the intentionally limited CLI, the richer web UI pages, project storage layout, compile behavior, marks, config, and response notes. Main risk: if command behavior changes again, README examples and UI descriptions will need to be kept in sync.
